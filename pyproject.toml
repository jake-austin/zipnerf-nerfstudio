[project]
name = "zipnerf"
version = "0.1.0"

dependencies=[
    "nerfstudio==0.2.2"
]

[tool.setuptools.packages.find]
include = ["zipnerf"]

[project.entry-points.'nerfstudio.method_configs']
zipnerf = 'zipnerf_config:zipnerf_method'
